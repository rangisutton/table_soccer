export const FIELD_SIDES = 16;
export const FIELD_RADIUS = 280;
export const COIN_RADIUS = 18;
export const GOAL_SPOKE_LENGTH = 50;
export const GOAL_GAP = 80; // width of opening between the two spokes
export const WIN_GOALS = 3;
export const CANVAS_WIDTH = 800;
export const CANVAS_HEIGHT = 800;

// Kickoff triangle layout (distances from kick coin)
export const KICKOFF_SPREAD = 36; // spacing between coins in triangle (tighter)
